[project]
authors = [{name = "hirschmann"},
           {name = "Volkoshkursk", email = "zaz-pagani@inbox.ru"}]
dependencies = ["matplotlib>=3.10,<4"]
name = "pid-autotune"
requires-python = ">= 3.11"
version = "0.1.0"

[build-system]
build-backend = "hatchling.build"
requires = ["hatchling"]

[tool.pixi.workspace]
channels = ["conda-forge"]
platforms = ["linux-64"]

[tool.pixi.pypi-dependencies]
pid_autotune = { path = ".", editable = true }

[tool.pixi.tasks]

